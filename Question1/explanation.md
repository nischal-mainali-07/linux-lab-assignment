id: Displays the current logged-in username and all groups the user belongs to.
pwd:Prints the present working directory
ls -l: Lists files and directories with permissions, owner, size, and timestamp.
echo "Linux user environment verified" > user_info.txt: Creates user_info.txt and writes the given text into it.
wc -c user_info.txt: Displays the number of characters present in the file.
man mkdir: Opens the manual page for the mkdir command.
mkdir -p dir1/dir2: -p creates parent directories automatically if they don’t exist.
ls ~: Lists all contents of the home directory alphabetically.
grep "admin" log.txt: Searches for the word admin and shows only matching lines.
uname -r: Displays the Linux kernel version currently running.
ping -c 4 www.google.com: Sends 4 ICMP packets to check network connectivity.
uptime: Shows system running time, logged-in users, and load average